  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.

## Deploy to GitHub Pages

The repository is configured to deploy automatically from the `master` branch via GitHub Actions.

In GitHub, set the repository Pages source to `GitHub Actions`. The workflow builds the app with the correct `/eco-descarte/` base path and publishes the `dist` folder.
  